on:
  workflow_call:
    inputs:
      image:
        required: true
        type: string

jobs:
  call:
    name: Call
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "${{ github.event_name }}"
          echo "${{ matrix.image }}"
          echo "${{ inputs.image }}"
